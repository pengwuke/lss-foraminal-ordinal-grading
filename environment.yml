name: lss-protocol-v3
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - opencv
  - pip
  - pip:
      - torch
      - torchvision
      - timm
      - albumentations
      - pillow
# Before release, export exact versions from the successful environment.
